
<div class='container'>

	<div class="page-header">
	  <h1>Make some changes to the ugly guy!<small></small></h1>
	</div>
	<div>
		<%= form_for @creature do |f| %>
			<%= f.text_field :name %>
			<%= f.text_area :description %>
			<%= f.submit 'Save the Monster!' %>
			<% end %>
	</div>

</div>


